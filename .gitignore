# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.eslintcache

# Build output
dist/
dist-ssr/
*.local    # Ojo: ignora cualquier archivo que termine en .local (útil si usas configs locales)

# Dependencies
node_modules/

# Pack artifacts
*.tgz

# Env files (mantén solo la plantilla)
.env
.env.*
!.env.example
# Redundante pero permitido si te gusta verlo explícito:
.env.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Tests / Coverage
coverage/
.nyc_output/

# TypeScript (si usas TS)
*.tsbuildinfo

# PNPM (si usas pnpm)
.pnpm-store/

# Cypress (si usas)
cypress/screenshots/
cypress/videos/

# Vercel (si usas)
.vercel/

# Supabase CLI (solo si lo usas en este repo)
.supabase/
supabase/.temp/
